import { BeforeAfterRail } from "@/components/sections/BeforeAfterRail";

/** Mobile before/after — the AI-staging rail sized for the phone column. */
export function BeforeAfter() {
  return <BeforeAfterRail height={260} cardWidth={320} className="mt-[18px]" />;
}
