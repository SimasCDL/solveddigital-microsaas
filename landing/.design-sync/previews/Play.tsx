import { Play } from "videotour-funnel";

// Play glyph — inherits color via `currentColor`, size via h-/w- utilities.
export const Default = () => <Play className="h-10 w-10 text-ink" />;

// Brand accent tone.
export const Accent = () => <Play className="h-10 w-10 text-accent" />;
