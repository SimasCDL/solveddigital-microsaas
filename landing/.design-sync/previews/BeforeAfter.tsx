import { BeforeAfter } from "videotour-funnel";

// Draggable before/after on the night background — flat "before" photo layer vs.
// the real tour video (AFTER). Renders the full section: heading, slider frame,
// Before/After badges, drag handle, and the transform caption.
export const Default = () => <BeforeAfter />;
