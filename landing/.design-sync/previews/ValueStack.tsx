import { ValueStack } from "videotour-funnel";

// "What's included" value stack — left framing column (eyebrow, heading, pitch,
// CTA) beside a checked list of everything bundled into the flat price.
export const Default = () => <ValueStack />;
