import { Hero } from "videotour-funnel";

// The landing hero — headline, sub, primary CTA, and proof stats. Renders the
// real section at desktop width (see cfg.overrides.Hero viewport).
export const Default = () => <Hero />;
