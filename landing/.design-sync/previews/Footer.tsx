import { Footer } from "videotour-funnel";

// Site footer — wordmark, section links, and copyright on the cream
// background. Zero-prop; renders its own demo content.
export const Default = () => <Footer />;
