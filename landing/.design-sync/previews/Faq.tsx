import { Faq } from "videotour-funnel";

// FAQ section — accordion-style list of common questions about the product.
// Zero-prop; renders its own demo content (see cfg.overrides.Faq viewport).
export const Default = () => <Faq />;
