import { Nav } from "videotour-funnel";

// Sticky pill nav — logo, section links, and the primary CTA in a rounded,
// blurred cream bar. Zero-prop; renders its own demo content.
export const Default = () => <Nav />;
