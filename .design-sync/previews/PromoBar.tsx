import { PromoBar } from "videotour-funnel";

// Thin founding-launch promo bar with an evergreen 24h countdown on the deep
// ink background. Zero-prop; renders its own demo content.
export const Default = () => <PromoBar />;
