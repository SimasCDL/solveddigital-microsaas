import { HowItWorks } from "videotour-funnel";

// Three-step "made easy" section — numbered step cards (upload, wait, post),
// the section heading, the primary CTA, and the reassurance line below it.
export const Default = () => <HowItWorks />;
