import { ProofStats } from "videotour-funnel";

// Three-stat proof row — value-based proof points in the accent display
// font. Zero-prop; renders its own demo content.
export const Default = () => <ProofStats />;
