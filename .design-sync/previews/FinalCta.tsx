import { FinalCta } from "videotour-funnel";

// Closing call-to-action — headline, sub, primary CTA, and proof chips.
// Zero-prop; renders its own demo content (see cfg.overrides.FinalCta viewport).
export const Default = () => <FinalCta />;
