import { Pricing } from "videotour-funnel";

// Pricing section — two packs (single $47 / trio $94 with "Most popular"
// badge), each with price, features, and CTA. Zero-prop; reads pricing data
// internally (see cfg.overrides.Pricing viewport).
export const Default = () => <Pricing />;
