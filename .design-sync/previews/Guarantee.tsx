import { Guarantee } from "videotour-funnel";

// Risk-reversal guarantee band on the dark (night) background.
// Zero-prop; renders its own demo content (see cfg.overrides.Guarantee viewport).
export const Default = () => <Guarantee />;
