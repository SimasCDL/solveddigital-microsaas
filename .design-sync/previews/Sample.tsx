import { Sample } from "videotour-funnel";

// "The work" showcase on the night background — eyebrow, heading, intro copy,
// and a marquee row of vertical tour cards with play buttons and durations.
export const Default = () => <Sample />;
